{
  "name": "packt-questions",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {},
  "scripts": {
    "deploy": "git push origin master && git push heroku master",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "heroku-prebuild": "echo Prebuild started...",
    "heroku-postbuild": "echo Postbuild started...",
    "preinstall": "cd client && npm install",
    "postinstall": "cd client && ng build --prod ---aot"
  },
  "author": "",
  "license": "ISC"
}
